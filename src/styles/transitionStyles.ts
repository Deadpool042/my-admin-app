// src/styles/transitionStyles.ts
const transitionStyles = {
  position: "relative",
  overflow: "hidden",
  transition: "opacity 0.5s ease, transform 0.5s ease",
  opacity: 1,
  transform: "translateY(0%)"
};

export default transitionStyles;
